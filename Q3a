import sympy as sp
x = sp.Symbol('x')
sp.integrate(sp.sin(x)**3*sp.cos(x)**2,x)

from scipy.integrate import quad
import numpy as np

def f(x):
  return sp.sin(x)**3*sp.cos(x)**2

i = quad(f,0,2*np.pi)
print(i[0])
